import React from 'react';
import Loader from 'react-js-loader';

const LoadingState = () => {
    return (
        <Loader type="spinner-default" bgColor='rgb(126, 34, 206)' color='rgb(126, 34, 206)' size={100} />
    )
}

export default LoadingState;